# snippets/healthz.conf — internal health endpoint for the Dockerfile
# HEALTHCHECK. Include INSIDE a server { } block on port 80 (or any).

location = /healthz {
    access_log off;
    add_header Content-Type text/plain;
    return 200 "ok\n";
}
