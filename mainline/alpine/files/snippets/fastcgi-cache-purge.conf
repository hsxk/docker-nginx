# snippets/fastcgi-cache-purge.conf — endpoint for ngx_cache_purge.
# Include INSIDE a server { } block. Restrict to trusted IPs.

location ~ ^/fcache-purge(/.*) {
    allow 127.0.0.1;
    allow ::1;
    deny  all;

    fastcgi_cache_purge wpcache "$scheme$request_method$host$1";
}
